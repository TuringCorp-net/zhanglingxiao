# MosaicCompress 反馈报告

> 对黎叔首个开源项目 `mosaic-compress` 的理解与反馈
> 基于 README + 中英文设计文档 + 源码 + 28/28 通过测试

---

## 一、我的理解

### 一句话概括

**MosaicCompress 是一个纯函数，它在 LLM 多轮对话中模拟人类的"自然遗忘曲线"，把无尽膨胀的上下文窗口锁定在一个恒定的尺寸上——而且用户完全感知不到 Session 的存在。**

### 三层"遗忘"模型

| 区域 | 轮次范围 | 处理方式 | 消息数 |
|------|---------|---------|--------|
| **Heavy 区**（远古） | 第 1 轮 ~ 第 R-heavyStart 轮 | 递归合并为 1 条用户摘要 + 1 条助手确认 | **2 条** |
| **Light 区**（稍远） | 第 R-heavyStart+1 ~ R-lightStart 轮 | 逐条蒸馏去水分，角色/顺序/数量不变 | **20 条**（10 轮） |
| **Raw 区**（近期） | 第 R-lightStart+1 ~ R 轮 | 完整保留 | **60 条**（30 轮） |
| **合计稳态** | — | — | **82 条**（+1 系统提示） |

### 核心机制

- **防抖动（Anti-jitter）**：压缩不在每轮触发，只在窗口边界轮次触发（默认每 10 轮），用户感知到的延迟 <=1-2s，其他轮次零开销
- **递归合并（Recursive Merge）**：Heavy 块不是累积的——上一轮的 Heavy 输出和最新的 Light 块合并，重新压缩为 2 条消息。所以 Heavy 块永远只有 1 个
- **LLM 自然收敛**：由于 `max_tokens` 约束，Heavy 块的 token 数存在物理上限；随着覆盖轮次越来越远，可提炼的新信息越来越少，token 数自动收敛
- **优雅降级**：LLM 调用失败时返回原始消息，对话不中断

### 数学美感

**关键数字：82**。从第 60 轮到第 15,000 轮，消息数始终 82 ± 18。压缩率在这个区间从 43.8% 自动趋近 99.8%。每轮新对话的平均边际成本趋近于 0。

---

## 二、设计亮点

### 1. "消灭 Session" 的哲学

这是最让我眼前一亮的设计决策。几乎所有同类方案（滑动窗口、摘要压缩、向量检索）都在"管理 Session"，而 MosaicCompress 的出发点是——**用户为什么需要知道 Session 是什么？** 这个视角转换意味着：不是用技术去优化一个用户困惑的概念，而是从根本上消除这个概念。这是"UX 驱动架构"的顶级实践。

### 2. 纯函数 vs 有状态系统的抉择

选择 stateless + idempotent 是一条困难但正确的路。这意味着：

- 没有状态管理复杂度，没有持久化问题
- 可以嵌入任何 LLM 工作流（Agent、Chat、Writer）而无副作用
- 测试简单（mock LLM 后就是纯函数逻辑），这也是为什么 28 个测试全部通过
- 缓存友好：同一输入永远产生同一输出

### 3. 信任 LLM 胜过规则

不做 TF-IDF 评分、不做规则截断、不做关键词提取。你把原始对话给 LLM，让 LLM 自己判断"什么重要、什么可以省略"。这听起来简单，但细想：

- LLM 本身就是"压缩"的专业户——它训练时就压缩了互联网
- 用 LLM 判断 LLM 语境下的信息重要性，远比任何手工规则靠谱
- 而且，LLM 在不断发展（DeepSeek、GPT、Claude），方案天然随着模型进步而进步

### 4. 设计简洁到罕见

整个核心代码 **~200 行 TypeScript**（含注释）。一个文件 `src/index.ts`。没有依赖，没有复杂的配置，没有第三方包。这在 AI 基础设施项目里简直是一股清流。

---

## 三、与现有方案对比

| 维度 | Sliding Window | Summary Compression | MosaicCompress |
|------|---------------|-------------------|----------------|
| 用户行为 | 需手动"开始新对话" | 需手动"保存摘要" | **零操作** |
| 对话结构 | 丢失早期 | 完全丢失 | **保留完整的 role/顺序/因果链** |
| 信息密度 | 浪费（重复保留近期信息） | 压缩过粗丢失细节 | **分层：近期完整、远期间隔、极远摘要** |
| 实现复杂度 | 简单 | 中等 | **~200 行纯函数** |
| 上下文上限 | 有（命中即丢） | 有（摘要增长） | **恒定 82 条 ~33.7K tokens** |

---

## 四、一些可能改进的方向（纯属锦上添花）

> 这些不一定是建议——有些可能是设计取舍，有些可能是 v1.0 之后的路线图。

### 1. TypeScript 类型导出优化
目前 `main` 和 `types` 都指向 `src/index.ts`，发布到 npm 时建议用 `tsc` 编译为 `dist/` 目录，同时生成 `.d.ts` 声明文件。目前 tsconfig 的 `declaration: true` 还可以加上。

### 2. LLM Prompt 的国际化
Light/Heavy Compress 的 system prompt 目前是纯英文，对中文用户可能效果不够好。虽然设计上"保留原始语言"，但 compressed 输出可能受英文 prompt 影响产生中英混杂。建议加一个 `language` 参数。

### 3. 缓存策略
由于输入不变输出不变（idempotent），理论上可以对 `(role + lightStart + ...)` 做 memoize，避免同一轮次重复压缩。但这个可能放 v2.0 更合适。

### 4. 浏览器端兼容
目前 `src/index.ts` 是纯 Node/Edge 环境，浏览器端可能需要 polyfill（特别是 `Promise` 依赖）。可以加一个 `.browser.mjs` 入口。

### 5. CLI 工具
对于非 npm 用户，提供一个 `npx mosaic-compress` CLI 可以更方便地 demo 和测试。

---

## 五、潜在应用场景

- **AI 写作助手**（Story Elf、NovelCrafter）：无尽对话的创作 session，用户不需要关心"新建对话"
- **客服机器人**：长时间、多轮次的客服会话，上下文不超限
- **AI 教育/辅导**：学生与 AI Tutor 的长期对话，不丢学习进度
- **Agent 框架**：作为 Agent 记忆系统的"上下文密度管理"层，与持久记忆文件互补
- **本地 LLM 环境**：对上下文窗口有限的开源模型（如 8K/32K），MosaicCompress 是救命稻草

---

## 六、总体评价

**黎叔，这是你的第一个开源项目，但说实话——这个质量远超"第一个项目"的水平。**

- **选题精准**：戳中 LLM 应用开发的真痛点（上下文膨胀），但给出了一个超优雅的解
- **设计克制**：~200 行搞定了别人用 2000 行都做不好的事
- **文档完整**：README + CN/EN 设计文档，质量和条理性吊打一堆成熟开源项目
- **测试完备**：28 个测试，零 LLM 成本，覆盖了边界/异常/稳态全部场景
- **哲学统一**："让 Session 消失"这个核心思想贯穿代码到文档的每一行

如果硬要说最亮眼的地方，我觉得是 **"递归合并 + LLM 自然收敛"** 这个设计——用一个纯函数 + 递归，既避免了状态管理的复杂度，又利用 LLM 自身的输出约束自然解决了无限增长问题。简洁、优雅、有数学美。

🔥🔥🔥

等你发布 npm 了告诉我，我第一时间 `npm install mosaic-compress` 用上！
