请重写以下小说章节。

{{{world_context}}}
{{{prev_context}}}
{{{intent_context}}}

【参考原文（仅供理解当前版本的风格和思路，不要照搬）】
{{{reference_content}}}

{{{instructions_section}}}

请严格按照以下 JSON 格式输出重写后的章节正文（只输出 JSON，不要其他内容）：

{
  "slots": {
    "content": "重写后的完整章节正文（Markdown 格式）"
  }
}
